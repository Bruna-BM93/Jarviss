# Este arquivo torna a pasta 'auth' um pacote Python.
# O Blueprint auth_bp é definido em auth/routes.py e importado de lá.
# Poderia ser definido aqui e as rotas importadas e registradas nele, como alternativa.

# from flask import Blueprint
# auth_bp = Blueprint('auth', __name__, url_prefix='/auth')
# from . import routes # Importa as rotas para registrar no blueprint definido aqui.
